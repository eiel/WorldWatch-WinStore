<UserControl
    x:Class="WorldWatch.ClockControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WorldWatch"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid Width="280" Height="44" Background="#33000000">
    	<TextBlock TextWrapping="Wrap" Text="{Binding Place}" FontSize="13.333" Margin="10,5,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
    	<TextBlock TextWrapping="Wrap" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="104,0,18,3" Width="158" TextAlignment="Right" Text="{Binding Time}" d:IsHidden="True"/>
    	<TextBlock TextWrapping="Wrap" Text="{Binding Date}" FontSize="13.333" Margin="10,24,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
